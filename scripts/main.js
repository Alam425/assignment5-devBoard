const taskCount = provideInnerText("taskCount")
const taskCompletionCount = provideInnerText("taskCompletionCount")
const activityHistory = provideInnerText("activityHistory")

const title = document.querySelectorAll(".title")
const completedBtn = document.querySelectorAll(".completedBtn")

const time = formated[5] + " " + formated[6]